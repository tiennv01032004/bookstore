let createTransport;_fd5‍.x([["MailerModel",()=>MailerModel]]);_fd5‍.w("nodemailer",[["createTransport",["createTransport"],function(v){createTransport=v}]]);

       class MailerModel {
  constructor() {}

  static async sendMail(data) {
    try {
      const { to, html } = data;

      const transporter = createTransport({
        service: "gmail",
        auth: {
          user: "nguyenvantien.coder@gmail.com",
          pass: "agktjblvfbbxqmyo",
        },
      });

      const mailOptions = {
        from: "nguyenvantien.coder@gmail.com",
        to,
        subject: "Đăng ký tại Bookstore",
        html,
      };

      const info = await transporter.sendMail(mailOptions);
      return info;
    } catch (error) {
      throw error;
    }
  }
}
